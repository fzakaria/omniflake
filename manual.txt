# Flakes added by hand, outside GitHub search. See tools/manual.py.
# One per line: "owner/repo", or any flake ref Nix can fetch.
#
# Each group of entries is its own keep-sorted block, so a group keeps
# the comment that explains why those lines are here. `nix fmt` sorts
# them and `nix flake check` fails on a group left unsorted.

# keep-sorted start case=no
AdnanHodzic/auto-cpufreq
b3nj5m1n/xdg-ninja
dfrankland/envoluntary
emilien-jegou/oyui
fzakaria/nix-auto-follow
fzakaria/nixpkgs-multiverse
github:roman/nixDir/v3
guibou/tsui
helix-editor/helix
idursun/jjui
Mic92/direnv-instant
nikitawootten/flake-graph
nix-community/nix-direnv
nix-community/nix-unit
NotAShelf/flint
sini/files
# keep-sorted end

# Reported missing in issue #2. GitHub calls the primary language of these
# three Rust and Haskell, and none carries a nix topic, so no query in
# harvest.py can reach them.
# keep-sorted start case=no
hgaiser/moonshine
nix-community/nix-vscode-extensions
rafaelrc7/wayland-pipewire-idle-inhibit
# keep-sorted end

# Also from issue #2. These two are language:Nix and would be harvested but
# for the result cap: their star buckets hold more than 1000 repositories
# and are not sliced finely enough, so both fall past the end of the search.
# Listed here so they are indexed no matter what the search returns.
# keep-sorted start case=no
Whovian9369/aaru-nix-flake
Whovian9369/rom-properties-nix-flake
# keep-sorted end

# Also from issue #2, and found by search already: the theme's repository
# is named "nix", which classify.py reads as somebody's own machine
# configuration. Listing it here is what overrides that guess.
catppuccin/nix

# Reported missing in issue #3. The repository is named "flake", which is
# one of the names classify.py reads as somebody's own machine
# configuration, so search finds it and the classifier drops it.
nixified-ai/flake

# 367 indexed flakes declare an input called hyprland and none of them
# could reach it: GitHub calls the repository's primary language C++ and
# it carries no nix topic, so no query in harvest.py returns it. 38,000
# stars and a root flake.nix, never once a candidate.
hyprwm/Hyprland

# Tools written in something other than Nix, and shipping a flake anyway.
# GitHub calls these Rust, Go and C, and none of the three carries a topic
# harvest.py queries -- jj and patchelf carry no topics at all, and
# treefmt's are cli, formatter and treefmt. 36,000 stars between them and
# a root flake.nix each, never once a candidate.
# keep-sorted start case=no
jj-vcs/jj
NixOS/patchelf
numtide/treefmt
# keep-sorted end
